# 交接模板

把下面内容发给下一个聊天窗口即可：

```text
我现在有一个“vx sft 项目”代码仓库，目录结构里包含 `training/`、`configs/`、`ui/`、`docs/`。

请先帮我完成下面几件事：
1. 检查当前训练配置是否合理
2. 检查 UI 是否可以直接跑起来
3. 如果我要在本地继续训练，请给我最短操作步骤
4. 如果我要部署给别人用，请帮我整理成可发布版本

约束：
- 不要泄露聊天数据
- 不要把私有权重直接公开上传
- 尽量保留当前目录结构
```
